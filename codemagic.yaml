workflows:
  my-workflow:
    scripts:
      - name: say hello
        script: echo "Hello Codemagician!"

workflows:
  run-on-push-hook:
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: master
          include: false
      cancel_previous_builds: true
