Sample save files for tests and manual smoke runs. These are not production player saves.

Runtime saves still write to `UserData/` (see `Settings.DevSaveDirectory`).
You can delete these safely; tests create fresh saves when needed.
